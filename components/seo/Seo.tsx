import React from 'react';
interface SeoProps {
    title: string;
    description: string
}
const Seo: React.FC<SeoProps> = ({ title, description }) => {
    return (
        <head>

        </head>
    );
};

export default Seo;